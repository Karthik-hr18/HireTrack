/**
 * Recruiter Candidate Workspace
 *
 * This directory contains all components for the new workspace-style
 * recruiter experience. See RECRUITER_UX.md and RECRUITER_WIREFRAMES.md
 * for the full design specification, and UI_MIGRATION_PLAN.md for the
 * phased build-out plan.
 *
 * Directory structure:
 *   CandidateWorkspacePage.tsx  — top-level page shell (Phase 2)
 *   CandidateList.tsx           — left panel list (Phase 2)
 *   CandidateRow.tsx            — list row item (Phase 2)
 *   StageTabBar.tsx             — pipeline stage filter tabs (Phase 2)
 *   FilterDropdown.tsx          — filter pane (Phase 2)
 *   CandidateDetailPanel.tsx    — right panel shell (Phase 3)
 *   OverviewTab.tsx             — default tab (Phase 4)
 *   ActionCard.tsx              — stage-aware action surface (Phase 4)
 *   PipelineProgressBar.tsx     — pipeline step indicator (Phase 4)
 *   SchedulingCard.tsx          — 3-step inline scheduling (Phase 5)
 *   RejectionCard.tsx           — inline rejection form (Phase 5)
 *   ResumeTab.tsx               — PDF viewer tab (Phase 4)
 *   TimelineTab.tsx             — activity feed tab (Phase 4)
 *   NotesTab.tsx                — notes compose + feed (Phase 4)
 *   InterviewsTab.tsx           — interview list tab (Phase 4)
 *   ScorecardsTab.tsx           — scorecard display + admin form (Phase 4)
 */

// Exports will be added here as each phase is implemented.
export {};
